# 2026-02-23

## Smart Receipt Scanner Mock 作成

**Context:** マスターが #lab で slack-for-idea リポジトリへの追加方法を質問

**Action:**
- `https://github.com/takasaka-ctrl/slack-for-idea` をクローン
- CLAUDE.md を読んで追加ルールを理解:
  - `projects/<project-name>/` に静的モックを配置
  - `projects/index.html` にカード追加
  - ビルド不要の静的HTML維持
- Smart Receipt Scanner のモックを作成:
  - `projects/smart-receipt-scanner/index.html` - GPT-4 Vision による領収書AI読み取りUI
  - `projects/smart-receipt-scanner/README.md` - 説明・PDCAスコア (7.7)
  - `projects/index.html` にカード追加
- Git commit 完了 (`760760a`)
- Push は認証エラー（マスターに手動プッシュを依頼）

**Tech Stack:**
- Tailwind CSS
- Space Grotesk / DM Sans フォント
- 静的HTML/CSS/JS

**Learning:**
- slack-for-idea リポジトリは「Slackで議論したアイデアのUIモックギャラリー」
- ビルドステップを必要としない純粋な静的サイト構造
- Git config が未設定だったので `user.name` / `user.email` を設定

**Result:** モック完成、ローカルコミット済み、GitHub push待ち

## PixelLab API でキャラクター画像生成

**Context:** マスターが #imagelab で「APIで画像生成して」とリクエスト

**Action:**
- PixelLab API v2 ドキュメントを調査 (`https://api.pixellab.ai/v2/llms.txt`)
- `/v2/create-character-with-4-directions` エンドポイントを使用
- 小さな魔法使いの見習いキャラクター（chibi スタイル、4方向、64x64px）を生成開始

**Job Details:**
- **Job ID:** `fdb17956-7f3d-44f2-832e-2cf9a4c53e22`
- **Character ID:** `55d3b86a-c827-43bb-941b-c6164937241b`
- **Status:** 処理中 (async)
- **Description:** purple pointed hat with golden star, light blue robe, wooden wand with glowing tip, chibi proportions
- **Specs:** 64x64px, 4 directions (south/west/east/north), single color black outline, basic shading, medium detail, low top-down view

**Learning:**
- PixelLab API v2 は非同期処理（background jobs）
- Character creation は常に `async_mode: true`
- 完了後は `/v2/background-jobs/{job_id}` または `/v2/characters/{character_id}` で確認可能
- ZIP ダウンロードは `/v2/characters/{character_id}/zip` から取得

**Next Steps:** ジョブ完了後、キャラクターデータとダウンロードリンクを取得

## ディスク容量クリーンアップ

**Context:** #imagelab でディスクフルメッセージ → マスターが #setting で質問

**Problem:** Docker overlay filesystem が 100% 使用 (19GB)

**Solution:**
- `lofi-pixel/` (687MB) 削除
- `remotion-lofi-pixel/` (215MB) 削除
- `ffmpeg/` (77MB) 削除
- **合計:** 978MB 解放 → 95% 使用率に改善

**Learning:**
- これは EC2 上の Docker コンテナの overlay filesystem
- `/app/node_modules` が 1.6GB あるが削除は危険
- 根本的な解決策は EBS ボリューム拡張

## OpenClaw アップデート試行

**Context:** マスターが OpenClaw をアップデートしたいと報告

**Status:** 未完了
- 現在バージョン: **2026.2.9** (変わらず)
- コンテナは再起動されたが、イメージは古いまま

**Next Steps:**
- EC2 で `docker pull openclaw/openclaw:latest` の出力確認が必要
- `Already up to date` なら 2026.2.9 が最新版
- 新しいバージョンが pull されていれば、コンテナ再作成が必要

## 新チャンネル計画: #focus または #compass

**Purpose:** 毎朝、Vault のメモを全て見て以下を配信
- 今日やるべきタスク
- 考えなきゃいけないこと (決断が必要な事項)
- 進行中プロジェクトの状況 (#lab, #imagelab など)
- 近日予定 (面接準備など)

**Frequency:** 朝1回

**Candidate Names:**
- **#focus** - 今日フォーカスすべきこと (シンプル)
- **#compass** - 方向性・進路確認 (航海メタファー)

**Status:** チャンネル名選定待ち

## #lab ワークフロー確認

**Context:** マスターが #lab の目的を再確認

**Clarification:**
- OpenClaw (Athena) がやるべきこと: 市場調査 + PDCA → **要件MDファイル生成まで**
- Claude Code Agent への実装依頼は **不要**
- 企画書を `20-Projects/` に保存したら完了

**Current Behavior:**
- Phase 2 完了後、Slack に企画書全文 + `@Claude` メンションで実装依頼している

**Action Required:** `/home/node/.openclaw/workspace/skills/idea-workflow/SKILL.md` の最終ステップを修正
- Slack 投稿 + Claude メンションを削除
- 企画書保存で完了とする

## PixelLab MCP: くノ一 (Kunoichi) スプライト生成

**Context:** マスターが #setting でくノ一（女性忍者）のスプライトを依頼

**Action:**
- MCP PixelLab (`create_map_object`) を使用
- 200×200px、透過背景、ピクセルアートスタイル
- **Aesthetic:** 和の茶道的な休息シーン
  - 暗い紺色の忍び装束、赤いアクセント
  - あぐらで座り、茶碗を両手で包む
  - 目を閉じた穏やかな表情
  - 森の自然パレット（16色、ダークグリーン・ネイビー・赤・茶）
  - 明確な黒アウトライン、シャープなピクセル（アンチエイリアシングなし）

**Result:**
- **Object ID:** `ac485f8c-c7b3-425b-840f-bed03c1e4b89`
- 生成成功、透過PNG取得済み
- ダウンロードリンク: `https://api.pixellab.ai/mcp/map-objects/{object_id}/download`
- **有効期限:** 8時間

**Learning:**
- `create_map_object` は単一の高品質スプライトに適している（max 400×400px）
- 複雑な美的要求（色パレット、ポーズ、表情、文化的要素）も指定可能
- 透過背景は自動的に処理される
- 以前のサイバーパンク猫少女（0b5eadf1-c0cb-4658-b7af-f5d4e5c2383f）と同じワークフロー
  - キュー → 60秒待機 → 取得
